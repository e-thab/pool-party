[gd_resource type="Shader" format=2]

[resource]
code = "shader_type canvas_item;

uniform float rate = 1.0;

varying vec2 v;

void vertex(){
	if (UV.x > 0.5)
		VERTEX += vec2(15.0, 15.0);
//	VERTEX *= abs(sin(x));
}"
